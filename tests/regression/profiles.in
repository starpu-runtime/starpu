# Build configuration
--enable-coverage
# Execution configuration

# Build configuration
--enable-debug --enable-verbose
# Execution configuration
STARPU_LOGFILENAME=starpu_run.log
#
# Build configuration

# Execution configuration
STARPU_NCUDA=0
# Execution configuration
STARPU_NCUDA=1
# Execution configuration
STARPU_SCHED=ws
# Execution configuration
STARPU_SCHED=prio
# Execution configuration
STARPU_SCHED=no-prio
# Execution configuration
STARPU_SCHED=dm
# Execution configuration
STARPU_SCHED=dmda
# Execution configuration
STARPU_SCHED=random
# Execution configuration
STARPU_SCHED=eager
# Execution configuration
STARPU_SCHED=dmda STARPU_SCHED_ALPHA=10 STARPU_SCHED_BETA=15
# Execution configuration
STARPU_CALIBRATE=1
# Execution configuration
STARPU_PREFETCH=1
#
# Build configuration
--disable-cuda
# Execution configuration

#
# Build configuration
--disable-opencl
# Execution configuration

