ifeq (@USE_FXT@,yes)
	OPTTARGET+= fxt.o
endif

ifneq (@BLAS_LIB@,none)
	OPTTARGET+= blas.o
endif

CFLAGS+= -I../

OBJS := hash.o htable32.o mutex.o rwlock.o timing.o $(OPTTARGET)

all: $(OBJS)

%.d: %.c
	$(CC) $(CFLAGS) $< -MM -o $*.d

-include $(OBJS:.o=.d)

clean:
	@rm -f *.o *.d *.gcno *.gcda 
